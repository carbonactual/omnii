# OMNII PLUGIN MODEL

Plugins are isolated implementation modules that connect external functionality to OMNII contracts.

Plugins receive only declared capabilities and resources, execute within explicit authority, and expose health and provenance.

A plugin may extend behavior but cannot silently alter canonical semantics or governance.