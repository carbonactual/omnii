# OMNII LIFECYCLE ENGINE

The lifecycle engine applies registered lifecycle definitions to canonical objects and runtime resources.

## Functions
`instantiate → activate → operate → suspend/restrict → resume → retire/deprecate`

Lifecycle definitions are schema-aware, versioned, authority-bound, and observable. Lifecycle transitions produce universal events and must preserve continuity and provenance.