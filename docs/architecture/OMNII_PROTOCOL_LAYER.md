# OMNII PROTOCOL LAYER

Defines common envelopes, identifiers, version negotiation, error semantics, event/command/query conventions, capability negotiation, authentication context, correlation, and provenance.

Protocols permit heterogeneous implementations while preserving universal semantics.

Compatibility is explicit; unknown fields and states must not be silently discarded.