# OMNII SECURITY ARCHITECTURE

Security is a constitutional runtime property spanning identity, access, capabilities, data, secrets, isolation, tenants, and service trust.

## Security Path
`authenticate → identify → authorize → isolate → execute → observe → revoke/recover`

Security controls are layered and fail closed at consequential boundaries where safe to do so.