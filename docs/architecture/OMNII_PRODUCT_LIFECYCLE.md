# OMNII PRODUCT LIFECYCLE

`proposed → designed → validated → registered → deployed → operating → restricted/suspended → evolved → deprecated → retired`

Each transition preserves version, dependencies, governance, security, evidence, and continuity.

Product retirement must address data, users, commitments, integrations, resources, and successor mappings.