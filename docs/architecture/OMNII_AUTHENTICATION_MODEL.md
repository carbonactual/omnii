# OMNII AUTHENTICATION MODEL

Authentication establishes evidence that a subject controls or represents a claimed identity.

## Requirements
Credential/attestation validation, freshness, challenge where appropriate, revocation, session lifecycle, device/workload identity, and provenance.

Authentication does not grant capability or authority; those are evaluated separately.