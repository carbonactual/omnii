# OMNII SEMANTIC LAYER

Provides shared meanings, vocabularies, canonical types, relationships, mappings, constraints, and versioned terminology across OMNII.

Semantic mappings distinguish equivalence, specialization, translation, approximation, and conflict.

The semantic layer prevents products and integrations from creating incompatible meanings for the same canonical concept.