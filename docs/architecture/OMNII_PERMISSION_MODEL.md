# OMNII PERMISSION MODEL

Permissions are runtime decisions over identity, capability, authority, resource, context, policy, and risk.

## Decision
`allow | allow_with_conditions | require_authorization | deny | quarantine`

## Rules
Permissions are least-privilege, explicit, revocable, auditable, and evaluated at consequential execution boundaries. A permission grant cannot exceed the authority that issued it.