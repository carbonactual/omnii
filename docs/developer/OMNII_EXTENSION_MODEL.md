# OMNII EXTENSION MODEL

Extensions add governed capabilities, object types, adapters, views, workflows, or integrations without redefining constitutional primitives.

Every extension declares identity, owner, contracts, dependencies, permissions, lifecycle, provenance, compatibility, and removal behavior.

Extension admission is validated before registration.