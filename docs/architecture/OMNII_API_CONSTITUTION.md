# OMNII API CONSTITUTION

Establishes universal API rules shared by every OMNII participant.

## Invariants
Stable identifiers, explicit versions, typed contracts, authentication, authorization, provenance, policy enforcement, idempotency where required, bounded payloads, error semantics, observability, and compatibility guarantees.

An API endpoint cannot create authority or redefine a canonical object.