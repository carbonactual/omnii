# OMNII EXECUTION API

Exposes intent submission, action preparation, capability resolution, resource resolution, authorization, execution, outcome, and state observation.

The API distinguishes requested action from completed effect and emits evidence for consequential execution.

Security and governance checks remain mandatory at execution boundaries.