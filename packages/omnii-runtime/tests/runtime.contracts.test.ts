import assert from "node:assert/strict";
import test from "node:test";
import { AgentRuntime } from "../src/agent-runtime";
import { AbbaRuntime } from "../src/abba-runtime";
import { EventStore, StateMachine } from "../src/event-runtime";
import { ExecutionRuntime } from "../src/execution-runtime";
import { GraphRuntime, Phase40GraphAdapter } from "../src/graph-runtime";
import { ObjectRuntime } from "../src/object-runtime";
import { RegistryRuntime } from "../src/registry-runtime";
import { RelationshipRuntime } from "../src/relationship-runtime";
import { TransitionRuntime } from "../src/transition-runtime";
import { WorkflowRuntime } from "../src/workflow-runtime";
import { AuditRuntime } from "../src/audit-runtime";
import { LedgerBoundary } from "../src/ledger-boundary";
import { Authority } from "../src/types";
import { MemoryPersistenceAdapter } from "../src/persistence";
const authority = (): Authority => ({ id: crypto.randomUUID(), subject: "human-1", scope: ["write", "execute"], capabilities: ["write", "execute"], issued_at: new Date().toISOString(), revocable: true, provenance: { source: "test" } });
const objectInput = () => ({ type: "test.object", status: "active", identity: { subject: "test" }, provenance: { source: "test" }, authority: { id: authority().id }, attributes: { name: "test" }, relationships: [], dependencies: [], capabilities: ["write"], resources: [] });
test("object persistence round trip and optimistic concurrency", async () => { const persistence = new MemoryPersistenceAdapter(); const runtime = new ObjectRuntime(persistence); const object = await runtime.create(objectInput()); assert.equal((await runtime.read(object.id))?.id, object.id); const updated = await runtime.update(object.id, { attributes: { name: "updated" } }, "1"); assert.equal(updated.version, "2"); await assert.rejects(() => runtime.update(object.id, { attributes: { stale: true } }, "1"), /version conflict/); assert.equal((await runtime.archive(object.id))?.status, "archived"); });
test("relationship persistence preserves provenance and traversal", async () => { const persistence = new MemoryPersistenceAdapter(); const objects = new ObjectRuntime(persistence); const relationships = new RelationshipRuntime(persistence); const a = await objects.create(objectInput()); const b = await objects.create(objectInput()); const relationship = await relationships.create({ type: "member_of", source: a.id, target: b.id, direction: "directed", status: "active", authority: { id: "auth" }, provenance: { source: "test" } }); assert.equal((await relationships.traverse(a.id))[0].id, relationship.id); assert.equal((await relationships.read(relationship.id))?.provenance.source, "test"); });
test("registry persists and resolves", async () => { const persistence = new MemoryPersistenceAdapter(); const registries = new RegistryRuntime(persistence); const record = { ...objectInput(), id: crypto.randomUUID(), version: "1", timestamps: { created_at: new Date().toISOString() }, authority: { id: "auth" }, provenance: { source: "test" } }; await registries.objects.register(record, "human-1"); await registries.objects.version(record.id, "2", "human-1"); await registries.objects.deprecate(record.id, "human-1"); assert.equal((await registries.objects.resolve(record.id))?.status, "deprecated"); assert.ok((await registries.objects.auditLog()).length >= 3); });
test("event immutability and correlation", async () => { const events = new EventStore(); const event = await events.append({ type: "TEST", actor: "human-1", subject: "o1", correlation_id: "corr-1", provenance: { source: "test" }, payload: {} }); assert.equal((await events.bySubject("o1"))[0].correlation_id, "corr-1"); assert.equal((await events.get(event.id))?.id, event.id); });
test("state transition requires authority and atomically persists its event", async () => { const persistence = new MemoryPersistenceAdapter(); const events = new EventStore(persistence); const runtime = new TransitionRuntime(events, new StateMachine([{ from: "pending", to: "active" }]), persistence); await assert.rejects(() => runtime.execute({ command: { type: "activate", actor: "human-1", subject: "o1", authority: { ...authority(), capabilities: [], scope: [] }, capability: "execute" }, from: "pending", to: "active" })); const result = await runtime.execute({ command: { type: "activate", actor: "human-1", subject: "o1", authority: authority(), capability: "execute", idempotency_key: "transition-1" }, from: "pending", to: "active" }); assert.equal(result.state, "active"); assert.equal((await events.all()).length, 1); assert.equal((await persistence.read("state", "o1"))?.version, "2"); });
test("execution persistence, authorization and idempotency", async () => { const persistence = new MemoryPersistenceAdapter(); const events = new EventStore(persistence); const runtime = new ExecutionRuntime(events, persistence); const input = { intentReference: "intent-1", actorIdentity: "human-1", authorityContext: authority(), capability: "execute", resources: [], dependencies: [], input: { ok: true }, provenance: { source: "test" }, idempotencyKey: "exec-1" }; const execution = await runtime.create(input); const duplicate = await runtime.create(input); assert.equal(duplicate.id, execution.id); await runtime.validate(execution.id); await runtime.authorize(execution.id); const completed = await runtime.run(execution.id, () => ({ result: "ok" })); assert.equal(completed.state, "completed"); assert.ok((await runtime.audit(execution.id)).length >= 1); });
test("graph persists and Phase 40 remains an adapter", async () => { const persistence = new MemoryPersistenceAdapter(); const objects = new ObjectRuntime(persistence); const relationships = new RelationshipRuntime(persistence); const events = new EventStore(persistence); const registries = new RegistryRuntime(persistence); const graph = new GraphRuntime(objects, relationships, events, registries); const a = await graph.addObject(objectInput()); const b = await graph.addObject(objectInput()); const relation = await graph.addRelationship({ type: "contains", source: a.id, target: b.id, direction: "directed", status: "active", authority: { id: "auth" }, provenance: { source: "test" } }); assert.equal((await graph.traverse(a.id))[0].id, b.id); assert.equal((await new Phase40GraphAdapter(graph).toNode(a.id, "civilization")).canonicalObjectId, a.id); assert.equal((await new Phase40GraphAdapter(graph).toRelation(relation.id)).canonicalRelationshipId, relation.id); });
test("workflow persists state and composes execution", async () => { const persistence = new MemoryPersistenceAdapter(); const events = new EventStore(persistence); const executions = new ExecutionRuntime(events, persistence); const workflows = new WorkflowRuntime(executions, events, persistence); workflows.register({ id: "wf", trigger: "manual", validate: () => true, plan: (context) => context, capability: "execute", authority: authority(), execute: () => ({ done: true }) }); const instance = await workflows.start("wf", { input: "x" }); const completed = await workflows.run(instance.id, "human-1"); assert.equal(completed.state, "completed"); assert.equal((await workflows.read(instance.id))?.state, "completed"); });
test("agent authorization, execution and revocation", async () => { const persistence = new MemoryPersistenceAdapter(); const events = new EventStore(persistence); const executions = new ExecutionRuntime(events, persistence); const agents = new AgentRuntime(executions, events, persistence); const agent = await agents.register({ identity: "agent-1", authority: authority(), capabilities: ["execute"], tools: [], context: {}, memory: {}, policyConstraints: {}, executionBoundary: {} }); await agents.verify(agent.identity); await agents.authorizeAgent(agent.identity, "execute"); const execution = await agents.execute(agent.identity, "execute", "intent-1", {}, () => ({ ok: true })); assert.equal(execution.state, "completed"); await agents.revoke(agent.identity); await assert.rejects(() => agents.execute(agent.identity, "execute", "intent-2", {}, () => ({ ok: true }))); });
test("ABBA cannot self-authorize and can delegate only brokered authority", async () => { const persistence = new MemoryPersistenceAdapter(); const events = new EventStore(persistence); const executions = new ExecutionRuntime(events, persistence); const agents = new AgentRuntime(executions, events, persistence); const agent = await agents.register({ identity: "agent-2", authority: authority(), capabilities: ["execute"], tools: [], context: {}, memory: {}, policyConstraints: {}, executionBoundary: {} }); await agents.verify(agent.identity); const abba = new AbbaRuntime({ request: () => null }, agents, events); await assert.rejects(() => abba.requestAuthority({ subject: agent.identity, capability: "execute", purpose: "test", resourceIds: [], context: {} })); });
test("audit and ledger are persisted separately from value and money", async () => { const persistence = new MemoryPersistenceAdapter(); const events = new EventStore(persistence); const audit = new AuditRuntime(events, persistence); const ledger = new LedgerBoundary(events, persistence); const record = await audit.record({ who: "human-1", what: "update", why: "test", authority: "auth-1", capability: "write", resource: ["r1"], object: "o1", result: "success" }); assert.equal((await audit.byObject("o1")).length, 1); assert.ok(record.when); const entry = await ledger.append({ transactionReference: "tx1", valueReference: "value1", moneyAmount: 10, moneyCurrency: "USD", provenance: { source: "test" }, authority: { subject: "human-1" } }); assert.equal(entry.valueReference, "value1"); assert.equal((await ledger.byTransaction("tx1")).length, 1); });
